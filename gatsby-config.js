module.exports = {
  siteMetadata: {
    title: `Url Shortener App`,
    description: `Simple url shortener build with lambda functions and Gatsby`,
    author: `@dantehemerson`,
  },
  plugins: [
    `gatsby-plugin-react-helmet`,
    `gatsby-plugin-styled-components`,
    `gatsby-plugin-sharp`,
    `gatsby-transformer-sharp`
  ],
}
